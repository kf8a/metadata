<% cache(:action_suffix => "glbrc") do %>
  <h1>GLBRC Sustainability Research Data Catalog</h1>
  <p id='datatable-intro'>
    This catalog is a collection of the data from the GLBRC sustainability research sites in Michigan and Wisconsin. Data are summarized by experiment. The Data Catalog summarizes each data table in order to allow the GLBRC community to better understand the data that have been collected and encourage collaboration. Please contact the lead investigator listed on the data table to request access to the data. Direct any questions to the <a href='mailto:glbrc.data@kbs.msu.edu'>GLBRC Sustainability Data Coordinator</a>. </p>
  <%= textilize @sponsor.data_use_statement if @sponsor %>

  <div id='search-form'>
    <%= form_tag('/datatables/search') do %>
    
      <div id='keyword-search'>
        <%= text_field_tag :keyword_list, @keyword_list || @default_value , {:id => 'keyworder'}  %>
      </div>  
      <div id='search-submit'>
        <%= submit_tag "Search Datacatalog"%>
      </div>
    <% end %>
  </div>
  <div>
    <h1 class='right'>Data</h1>
    <ul class='quickTree'>
        <%= render_study(:code => 'GIBC')%>
        <%= render_study(:code => 'GSCALE')%>
        <%= render_study(:code => 'GEXT')%>
        <%= render_study(:code => 'GSWITCH')%>
        <%= render_study(:code => 'GVAR')%>
        <%= render_study(:code => 'GPROD')%>
    </ul>
  </div>


  <% if signed_in? %>
    <li><a href='/uploads/new'>Uploads</a></li>
  <% end %>
<% end %>
