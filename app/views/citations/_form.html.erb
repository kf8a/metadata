  <%= form.inputs:citation_type %>
  <% @citation.authors.each do |author| %>
    <%= form.inputs :given_name, :sur_name, :for => author %>
  <% end %>

  <%= form.inputs :title, :abstract, :pub_year, :publication, :start_page_number, :ending_page_number, :periodical_full_name,
    :periodical_abbreviation, :volume, :issue, :isbn, :doi, :publisher_url, :full_text, :publisher, :address, :city %>
  <%= form.inputs :pdf , :as => :file%>
