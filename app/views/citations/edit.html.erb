<%= semantic_form_for @citation, :as => :citation, :url=> citation_path(@citation), :html => { :multipart => true } do |form| %>
  <%= render :partial => 'form', :locals => {:form => form } %>
  <%= form.actions %>
<% end %>
