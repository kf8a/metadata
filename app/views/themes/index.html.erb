
<div>
  <%= render 'theme_list', :theme_roots => @theme_roots %>
</div>

<%= form_for(:theme, :url => themes_path) do |f| %>
  <p>
    <label for='theme_name'>Theme Name</label>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>
