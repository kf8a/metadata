<h1><%= @venue.name.capitalize %> Meetings</h1>
<p>The KBS LTER Program hosts annual all scientist meetings at which LTER investigators present on-going research and discuss research findings.We also host shorter receptions and tours on an ad hoc basis.</p>
<p>Abstracts from past and current meetings are available below:</p>
<ul>
<% @meetings.each do |meeting| %>
<% if meeting.meeting_abstracts.size == 0 %>
	<li><%= meeting.title %> <%= meeting.date.to_s(:long) %><%= link_to 'edit', edit_meeting_path(meeting), {:class => 'edit'} if logged_in? %></li></li>
<% else %>
	<li><%= link_to "#{meeting.title}", meeting_path(meeting) %> <%= meeting.date.to_s(:long) %> <%= link_to 'edit', edit_meeting_path(meeting), {:class => 'edit'} if logged_in? %></li>
<% end %>
<% end %>
</ul>

<%= link_to 'new meeting', new_meeting_path() if logged_in? %>