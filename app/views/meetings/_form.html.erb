<fieldset>
  <%= f.label      :title %>
  <%= f.text_field :title, :size => '150'%>
</fieldset>
<fieldset>
  <%= f.label       :date, 'Start Date' %>
  <%= f.date_select :date %>
</fieldset>
<fieldset>
  <%= f.label       :date_to, 'End Date' %>
  <%= f.date_select :date_to %>
</fieldset>
<fieldset>
  <%= f.select :venue_type_id, venues %>
</fieldset>
<fieldset>
  <%= f.label     :announcement %>
  <%= f.text_area :announcement, :size => '150x20'%>
</fieldset>
